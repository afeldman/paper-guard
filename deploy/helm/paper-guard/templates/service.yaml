apiVersion: v1
kind: Service
metadata:
  name: {{ include "paper-guard.fullname" . }}
  labels:
    {{- include "paper-guard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "paper-guard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
